# Admin dashboard password
ADMIN_PASSWORD=admin123

# Server port (default 3847)
PORT=3847

# Persistent data root (Render disk mounts at /var/data). Default: __dirname, or /var/data if it exists.
# DATA_ROOT=/var/data
